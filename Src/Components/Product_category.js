import React from 'react'

function Product_category({header}) {
  return (
    <div className='bg-white p-10   w-[400px] z-30 lg:h-[300px] md:h-[300px] sm:h-[50]'>
      <p className=''>{header}</p>
    </div>
  )
}

export default Product_category
